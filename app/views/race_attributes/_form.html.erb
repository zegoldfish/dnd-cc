<%= form_for(@race_attribute) do |f| %>
  <% if @race_attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@race_attribute.errors.count, "error") %> prohibited this race_attribute from being saved:</h2>

      <ul>
      <% @race_attribute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
